Data Pipeline

This folder handles downloading, storing, and splitting historical OHLCV (Open, High, Low, Close, Volume) stock data for the trading environment.

Run download.py to download the data.
```bash
python -m download.py
```
